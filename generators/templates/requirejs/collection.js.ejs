/*global define*/

define([
  'underscore',
  'backbone',
  'models/<%= modelName %>'
], function (_, Backbone, <%= modelName %>Model) {
    'use strict';

    var <%= className %>Collection = Backbone.Collection.extend({
        url: '/<%= className.toString().toLowerCase() %>',
        model: <%= modelName %>Model
    });
    return <%= className %>Collection;
});
