/*global <%= appSlugName %>, Backbone*/

<%= appSlugName %>.Models = <%= appSlugName %>.Models || {};

(function () {
    'use strict';

    <%= appSlugName %>.Models.Doctor = Backbone.Model.extend({

        initialize: function () {
        },

        defaults: {
            _id: '',
            name: '',
            email: '',
            image: ''
        },

        validate: function (attrs, options) {
        },

        parse: function (response, options) {
            return response;
        }
    });

})();
