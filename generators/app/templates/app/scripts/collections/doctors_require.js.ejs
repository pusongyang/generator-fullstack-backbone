/*global define*/

define([
    'underscore',
    'backbone',
    'models/doctor'
], function (_, Backbone, DoctorModel) {
    'use strict';

    var DoctorsCollection = Backbone.Collection.extend({
        url: '/doctors',
        model: DoctorModel
    });

    return DoctorsCollection;
});
